<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">OmniCoInsurance</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if (currentUser) { %>
          <% if (currentUser.role === 'admin') { %>
            <li class="nav-item"><a class="nav-link" href="/admin/users">Users</a></li>
            <li class="nav-item"><a class="nav-link" href="/admin/policies">Pending Policies</a></li>
            <li class="nav-item"><a class="nav-link" href="/admin/claims">Pending Claims</a></li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link" href="/claims">Claims</a></li>
            <li class="nav-item"><a class="nav-link" href="/payments">Payments & Billing</a></li>
            <li class="nav-item"><a class="nav-link" href="/dashboard">Policies</a></li>
          <% } %>
        <% } %>
      </ul>
      <ul class="navbar-nav">
          <% if (currentUser) { %>
          <li class="nav-item"><span class="navbar-text me-3">Hello, <%= currentUser.full_name %></span></li>
          <% if (currentUser.role !== 'admin') { %>
            <li class="nav-item">
              <a class="btn btn-sm btn-primary me-2" href="/policies/new">Get a Quote</a>
            </li>
          <% } %>
          <li class="nav-item">
            <form action="/auth/logout" method="post" class="d-inline">
              <button class="btn btn-outline-light btn-sm" type="submit">Logout</button>
            </form>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

