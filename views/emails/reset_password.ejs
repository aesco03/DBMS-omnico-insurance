<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title><%= siteName %> — Password Reset</title>
    <style>
      body { font-family: Arial, sans-serif; color: #333; }
      .container { max-width: 600px; margin: 24px auto; padding: 24px; border: 1px solid #eee; border-radius: 8px; }
      .btn { display:inline-block; background:#007bff; color:#fff; padding:10px 16px; text-decoration:none; border-radius:6px; }
      .muted { color: #777; font-size: 0.9rem; }
    </style>
  </head>
  <body>
    <div class="container">
      <h2><%= siteName %> Password Reset</h2>
      <p>Hi <%= name || 'there' %>,</p>
      <p>We received a request to reset your password. Click the button below to set a new password. This link will expire in 1 hour.</p>
      <p><a class="btn" href="<%= resetUrl %>">Reset your password</a></p>
      <p>If you cannot click the button, copy-paste this URL into your browser:</p>
      <p class="muted"><%= resetUrl %></p>
      <hr />
      <p class="muted">If you didn't request a password reset, you can safely ignore this email.</p>
      <p class="muted">— The <%= siteName %> Team</p>
    </div>
  </body>
</html>
